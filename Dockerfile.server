FROM node:20-alpine

WORKDIR /app

# Install dependencies first (better layer caching)
COPY package*.json ./
RUN npm install --omit=dev

# Copy server source
COPY server/ ./server/

EXPOSE 5001

CMD ["node", "server/index.js"]
